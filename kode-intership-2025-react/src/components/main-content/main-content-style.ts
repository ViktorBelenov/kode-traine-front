import styled from "styled-components";

export const SEmployWrapper = styled.div`
    padding: 22px 16px;
    margin: 0 auto;
    width: 100%;


    background-color: ${(props) => props.theme.background};
`
